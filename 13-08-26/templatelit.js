let obj={
    name:"Pen",
    price:10
}
console.log("The cost of " + obj.name + " is $" + obj.price);//we can use , instead of +
console.log(`The cost of ${obj.name} is $${obj.price}`);
console.log(`sum is ${10+20}`);